pipeline {
      agent {
            dockerfile {
                filename 'Dockerfile'
                dir 'cicd/docker'
                label 'my-defined-label'
            }
        }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
