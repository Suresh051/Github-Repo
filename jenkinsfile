 pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo "Hello World! SURESH"
                sh "echo Hello from the shell"
                sh "hostname"
                sh "uptime"
            }
        }
    }
}
