pipeline {
      agent {
            dockerfile {
                filename 'Dockerfile'
                dir 'cicd/docker'
            }
        }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
