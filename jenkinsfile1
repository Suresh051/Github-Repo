pipeline {
      agent {
            dockerfile {
                filename 'Dockerfile'
                dir 'cicd/docker'
                label 'Docker-enabled'
            }
        }
    stages {
        stage('Test') {
            steps {
                sh '''
                    node --version
                    sfcc-ci client:auth $API_KEY $API_SECRET
                '''
            }
        }
    }
}
